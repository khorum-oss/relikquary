.DS_Store
.idea
**/build/**
.gradle/**
.kotlin/**
relikquary-store**/

# Embedded SQLite database (default persistence backend, feature 016)
relikquary.db
relikquary.db-*
*.sqlite

**/.gradle/**
node_modules/**

secret.properties

# Deployment env files hold real credentials; only deploy/.env.example (placeholders) is committed.
.env
deploy/.env
!**/*.env.example

# IntelliJ HTTP Client private environment (holds publish credentials)
http-client.private.env.json
**/http-client.private.env.json

jreleaser_public.asc
public.key
.serena
*.asc
*.gpg

# Local Maven-publish testing / secrets — never commit internal setup
**/build/test-repo/
publish-local.properties
*.jks